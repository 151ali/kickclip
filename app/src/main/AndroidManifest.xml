<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed for resolving short URL redirect chains via OkHttp -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kickclip">

        <!-- Main Activity: Provides onboarding, testing tools, and permissions -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Kickclip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 
          Share Target Activity:
          - Styled with translucent dialog theme so it renders as a floating bottom-sheet box over TikTok
          - Listens for ACTION_SEND with text/plain mime-type
          - Android Share Sheet will display 'Kickclip' when sharing text/links
        -->
        <activity
            android:name=".ShareActivity"
            android:label="@string/share_target_title"
            android:exported="true"
            android:theme="@style/Theme.Kickclip.Dialog"
            android:windowSoftInputMode="adjustResize"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="">
            <intent-filter android:label="@string/share_target_title">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

    </application>

</manifest>
